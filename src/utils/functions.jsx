import htmlTagData from "../data/htmltags";

const tagCountObject = {};

const tagChecklistCounter = function () {
  console.log(htmlTagData);
};

export default tagChecklistCounter;

// create empty object
// leap through HTML tag object

